seed: 20260821

paths:
  data_root: data/raw/uav_sead
  inventory: reports/p1/dataset_inventory.csv
  channels: reports/p1/core_channels.txt
  aligned_dir: data/processed/p2/aligned_npz
  aligned_manifest: reports/p2/aligned_manifest.csv
  feature_dir: data/processed/p11/features_leave_log_out
  report_dir: reports/p11/llo/p2

data:
  frequency_hz: 10.0
  window_size: 96
  stride: 8
  horizon: 12
  train_fraction: 0.70
  validation_fraction: 0.15
  protocol: leave_log_out
  batch_size: 256

model:
  n_estimators: 1600
  learning_rate: 0.025
  num_leaves: 64
  min_child_samples: 80
  subsample: 0.90
  colsample_bytree: 0.75
  reg_alpha: 0.20
  reg_lambda: 8.0
  early_stopping_rounds: 80
  class_weight: balanced

random_forest:
  n_estimators: 100
  max_features: sqrt
  min_samples_leaf: 2
  class_weight: balanced_subsample
  n_jobs: -1

experiment:
  seeds: [0]
  n_jobs: -1
